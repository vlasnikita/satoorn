import { templates } from 'Constants/templates'

const initialState = {
    templates
}

export default (state = initialState, { type, payload }) => {
    switch(type) {
        default:
            return state
    }
};